# Create a public subnet resource "aws_subnet" "public_subnet" { vpc_id = data.aws_vpc.selected_vpc.id cidr_block = var.public_subnet_cidr availability_zone = data.aws_vpc.selected_vpc.availability_zones[0] map_public_ip_on_launch = true tags = { Name = "Public Subnet" } } # Create a private subnet for the database resource "aws_subnet" "private_subnet" { vpc_id = data.aws_vpc.selected_vpc.id cidr_block = var.private_subnet_cidr availability_zone = data.aws_vpc.selected_vpc.availability_zones[1] tags = { Name = "Private Subnet" } }
